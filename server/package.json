{
  "name": "opencall-server",
  "version": "1.0.0",
  "description": "OpenCall Signal Server — zero dependency calling",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.17.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}